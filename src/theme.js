import { extendTheme } from "@chakra-ui/react";

const theme = extendTheme({
  colors: {
    black: "#111111",
    golden: "#CF9B21",
    yellow: "#FFFAEE",
    gray: "#E2E2E2",
  },
});

export default theme;
